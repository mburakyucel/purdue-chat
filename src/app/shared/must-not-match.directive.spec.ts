import { MustNotMatchDirective } from './must-not-match.directive';

describe('MustNotMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new MustNotMatchDirective();
    expect(directive).toBeTruthy();
  });
});
